<h2>{{@title}}</h2>

<ul>
    {{#each @people as |list-member|}}
        <li>
            <button type="button" {{on 'click' (fn this.showPerson list-member)}}>
                {{list-member}}
            </button>
        </li>
    {{/each}}
</ul>