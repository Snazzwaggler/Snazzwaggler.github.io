import { module, test } from 'qunit';
import { setupTest } from 'snazzwaggler/tests/helpers';

module('Unit | Route | a-list-two', function (hooks) {
  // setupTest(hooks);

  // test('it exists', function (assert) {
  //   let route = this.owner.lookup('route:a-list-two');
  //   assert.ok(route);
  // });
});
